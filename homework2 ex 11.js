var int = 2;
lastdigit= int% 10;
var last= int.toString().split('').slice(1).join('');
var firstdigit = (''+int)[0];
var res = last.toString().split('').slice(0,-1).join('');

if (lastdigit==int){
    console.log(int)
}else{ console.log(lastdigit+res+firstdigit)};



var int = 13;
lastdigit= int% 10;
var last= int.toString().split('').slice(1).join('');
var firstdigit = (''+int)[0];
var res = last.toString().split('').slice(0,-1).join('');

if (lastdigit==int){
    console.log(int)
}else{ console.log(lastdigit+res+firstdigit)};


var int = 895796;
lastdigit= int% 10;
var last= int.toString().split('').slice(1).join('');
var firstdigit = (''+int)[0];
var res = last.toString().split('').slice(0,-1).join('');

if (lastdigit==int){
    console.log(int)
}else{ console.log(lastdigit+res+firstdigit)};